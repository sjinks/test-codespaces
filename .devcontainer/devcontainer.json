{
    "name": "WordPress",
    "dockerComposeFile": "docker-compose.yml",
    "service": "nginx",
    "initializeCommand": "echo \"HOST_UID=$(id -u)\nHOST_GID=$(id -g)\" > .devcontainer/.env; env >> .devcontainer/.env"
}
